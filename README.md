# Football Analysis Android V1.0
Built directly from the latest V2.30 Giant source.
The complete V2.30 HTML/JavaScript is packaged as an Android WebView asset, preserving the existing app logic and localStorage.
Open in Android Studio, sync Gradle, then Build > Build APK(s).
